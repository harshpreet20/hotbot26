# ─── Copy this file to .env.local and fill in your values ───────────────────
# Architecture:
#   Next.js API routes → individual N8N workflow webhooks
#   WhatsApp / external callbacks → wa-incoming (router workflow)

# ── N8N Workflow Webhooks ─────────────────────────────────────────────────────
# Workflow 1 — Forms (Leads · Newsletter · Contact · Analytics)
N8N_WEBHOOK_FORMS=https://hotbotst.app.n8n.cloud/webhook/hotbotstudios-forms

# Workflow 2 — Chat (AI + Human Handoff)
N8N_WEBHOOK_CHAT=https://hotbotst.app.n8n.cloud/webhook/hotbotstudios-chat

# Workflow 3 — Voice Agent (Sarvam AI outbound call trigger)
N8N_WEBHOOK_VOICE=https://hotbotst.app.n8n.cloud/webhook/hotbotstudios-voice

# Workflow 4 — Leads Pipeline
N8N_WEBHOOK_LEADS=https://hotbotst.app.n8n.cloud/webhook/hotbotstudios-leads

# Workflow 5 — Analytics Pipeline
N8N_WEBHOOK_ANALYTICS=https://hotbotst.app.n8n.cloud/webhook/hotbotstudios-analytics

# Workflow 6 — Contact Pipeline
N8N_WEBHOOK_CONTACT=https://hotbotst.app.n8n.cloud/webhook/hotbotstudios-contact

# Workflow 7 — Newsletter Pipeline
N8N_WEBHOOK_NEWSLETTER=https://hotbotst.app.n8n.cloud/webhook/hotbotstudios-newsletter

# Workflow 8 — Blog Create & Publish
N8N_WEBHOOK_BLOG_CREATE=https://hotbotst.app.n8n.cloud/webhook/hotbotstudios-blog-create

# Workflow 9 — Blog Auth (Backdrop login)
N8N_WEBHOOK_BLOG_AUTH_URL=https://hotbotst.app.n8n.cloud/webhook/hotbotstudios-blog-auth

# Workflow 10 — Blog → Google Sheets Backup
N8N_WEBHOOK_BLOG_SHEETS=https://hotbotst.app.n8n.cloud/webhook/hotbotstudios-blog-sheets

# Misc — WhatsApp Incoming Router
N8N_WEBHOOK_WA_INCOMING=https://hotbotst.app.n8n.cloud/webhook/wa-incoming

# ── Google Analytics 4 ───────────────────────────────────────────────────────
NEXT_PUBLIC_GA_ID=G-5CNWV5X1KC

# ── App URL (used for OpenGraph metadata) ────────────────────────────────────
NEXT_PUBLIC_SITE_URL=https://hotbotstudios.com

# ── Blog / Backdrop admin ────────────────────────────────────────────────────
# Auth is handled by N8N (Workflow 9). The publish secret is used for
# legacy external tool access to blog API routes.
BLOG_PUBLISH_SECRET=change-me-strong-random-secret

# ── reCAPTCHA v3 (optional — forms work without it) ──────────────────────────
# 1. Go to https://www.google.com/recaptcha/admin/create
# 2. Choose reCAPTCHA v3 → add your domain (localhost for dev)
# 3. Copy Site Key (public) and Secret Key (server-only)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here
RECAPTCHA_SECRET_KEY=your_recaptcha_secret_key_here
